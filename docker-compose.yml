services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-bitnp_ideas}
      POSTGRES_USER: ${POSTGRES_USER:-bitnp_ideas}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-bitnp_ideas}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-bitnp_ideas} -d ${POSTGRES_DB:-bitnp_ideas}"]
      interval: 5s
      timeout: 3s
      retries: 20

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    environment:
      BITNP_IDEAS_CONFIG: /app/apps/backend/config.docker.yaml
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    environment:
      VITE_API_BASE_URL: /api/v1
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  postgres-data:
